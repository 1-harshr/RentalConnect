package com.harsh.rentalconnect.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand primary ─────────────────────────────────────────────────────────────
val Primary          = Color(0xFF1A56DB)
val PrimaryVariant   = Color(0xFF1347C0)
val OnPrimary        = Color(0xFFFFFFFF)

/** Very light blue — selected card background, map placeholder, nav indicator */
val PrimaryContainer    = Color(0xFFEFF6FF)
/** Light blue — property image placeholders, phone icon box, avatar backgrounds */
val PrimarySubtle       = Color(0xFFBFDBFE)
/** Mid blue — owner avatar background */
val PrimaryMid          = Color(0xFFDBEAFE)
/** Dark blue — text rendered on PrimarySubtle / PrimaryMid surfaces */
val OnPrimaryContainer  = Color(0xFF1E40AF)

// ── Background / Surface ──────────────────────────────────────────────────────
val Background        = Color(0xFFF9FAFB)
val Surface           = Color(0xFFFFFFFF)
/** Muted gray — photo-upload area, unselected icon box background */
val SurfaceMuted      = Color(0xFFF3F4F6)
val OnBackground      = Color(0xFF111928)
val OnSurface         = Color(0xFF111928)
val OnSurfaceVariant  = Color(0xFF6B7280)
/** Lighter gray used for uppercase section-header labels */
val SectionLabel      = Color(0xFF9CA3AF)

// ── Divider / outline ─────────────────────────────────────────────────────────
/** Subtle dividers and card borders */
val Divider           = Color(0xFFE5E7EB)
/** Text-field unfocused borders, pill outlines, photo-upload dashed border */
val OutlineSubtle     = Color(0xFFD1D5DB)

// ── Status: Occupied (amber) ──────────────────────────────────────────────────
val OccupiedContainer = Color(0xFFFEF3C7)
val OnOccupied        = Color(0xFF92400E)

// ── Status: Vacant / success (green) ─────────────────────────────────────────
val VacantContainer   = Color(0xFFD1FAE5)
val OnVacant          = Color(0xFF065F46)

// ── Tenant app accent ─────────────────────────────────────────────────────────
/** Dark green used as the top accent bar in the tenant-facing app */
val AccentGreen       = Color(0xFF1A7A4C)

// ── "Your home" badge ────────────────────────────────────────────────────────
val HomeBadgeBg       = Color(0xFFFFFBEB)

// ── Icon box tints (coloured squares in info rows) ────────────────────────────
val IconTintBlue      = Color(0xFFBFDBFE)   // phone
val IconTintAmber     = Color(0xFFFEF3C7)   // hometown
val IconTintPurple    = Color(0xFFEDE9FE)   // Aadhar ID
val IconTintGreen     = Color(0xFFD1FAE5)   // tenant-since / house number
val IconTintIndigo    = Color(0xFF6366F1)   // map location (owner detail)
val IconTintOrange    = Color(0xFFF59E0B)   // house number alternate (owner detail)

// ── Property image placeholder palette ───────────────────────────────────────
val PlaceholderBlue   = Color(0xFFBFDBFE)
val PlaceholderTeal   = Color(0xFF99F6E4)
val PlaceholderIndigo = Color(0xFFC7D2FE)
val PlaceholderAmber  = Color(0xFFFDE68A)

// ── Tenant avatar color palette ───────────────────────────────────────────────
val AvatarIndigo      = Color(0xFF4C6EF5)
val AvatarTeal        = Color(0xFF12B886)
val AvatarAmber       = Color(0xFFF59F00)
val AvatarPink        = Color(0xFFE64980)
val AvatarViolet      = Color(0xFF7950F2)
val AvatarCyan        = Color(0xFF1098AD)

// ── Error ─────────────────────────────────────────────────────────────────────
val Error             = Color(0xFFE02424)
val OnError           = Color(0xFFFFFFFF)
